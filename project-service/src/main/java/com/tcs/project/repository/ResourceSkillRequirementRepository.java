package com.tcs.project.repository;

import com.tcs.project.entity.ResourceSkillRequirement;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResourceSkillRequirementRepository extends JpaRepository<ResourceSkillRequirement, Long> {
}
